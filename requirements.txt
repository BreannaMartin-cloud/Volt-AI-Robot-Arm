# VOLT robot - Python dependencies (Raspberry Pi 4, Raspberry Pi OS)
#
# Install with:
#   pip3 install -r requirements.txt --break-system-packages
#
# opencv and numpy are often faster to install from apt on a Pi:
#   sudo apt install python3-opencv python3-numpy

adafruit-circuitpython-servokit>=1.3
luma.oled>=3.12
RPi.GPIO>=0.7
opencv-python>=4.8
numpy>=1.24
mediapipe>=0.10
vosk>=0.3.45
sounddevice>=0.4
